import { LevelSource } from './level_source';

export class Level {
  // TODO accept data array in ctor
  // separate pixel reading (client / browser needs separate things)
  // => pngjs in node, pixi in browser
  constructor(source: LevelSource) {}
}
